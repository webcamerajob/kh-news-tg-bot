# .github/workflows/parsepostv2.yml
name: Parse & Post Articles

on:
  push:
    branches: 
      - main
  workflow_dispatch:

env:
  # общие переменные, прокинутся в оба job’а
  POST_DELAY: 5
  BATCH_LIMIT: 50

jobs:

  parse:
    name: Парсинг → артефакты
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install parser requirements
        run: |
          if [ -f requirements-parser.txt ]; then
            echo "🛠 Installing parser deps"
            pip install -r requirements-parser.txt
          else
            echo "📦 No requirements-parser.txt, skipping"
          fi

      - name: Run parser
        run: |
          echo "▶️ Запускаем main.py без --out-dir"
          python3 main.py

      - name: Debug parser output
        run: |
          echo "→ Дерево корня после парсера:"
          ls -R .

      - name: Upload parsed-content
        uses: actions/upload-artifact@v4
        with:
          name: parsed-content
          path: parsed/articles

  post:
    name: Публикация новых статей
    needs: parse
    runs-on: ubuntu-latest
    env:
      TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHANNEL: ${{ secrets.TELEGRAM_CHANNEL }}

    steps:
      - name: Checkout repo (с пустым catalog.json)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install poster requirements
        run: |
          echo "🛠 Installing poster deps"
          pip install httpx pillow

      - name: Download parsed-content
        uses: actions/download-artifact@v4
        with:
          name: parsed-content
          path: parsed/articles

      - name: Debug parsed-dir
        run: |
          echo "→ Структура parsed/articles в post-job:"
          ls -R parsed/articles

      - name: Run poster
        run: |
          echo "▶️ Запускаем poster.py"
          python3 poster.py \
            --parsed-dir parsed/articles \
            --state-file articles/catalog.json \
            --limit $BATCH_LIMIT

      - name: Commit updated catalog.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add articles/catalog.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update catalog after posting"
            git push
          else
            echo "✅ catalog.json unchanged — skipping commit"
          fi
