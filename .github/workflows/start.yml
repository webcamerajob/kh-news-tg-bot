name: Parse & Post Articles
permissions:
  contents: write
on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  POST_DELAY: 5
  BATCH_LIMIT: 50
  PYTHON_VERSION: "3.10"  # Фиксированная версия Python

jobs:
  parse:
    name: Парсинг → артефакты (WireGuard)
    runs-on: ubuntu-latest
    env:
      WG_CONFIG: ${{ secrets.WG_CONFIG }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔐 Setup WireGuard
        run: |
          sudo apt-get update
          sudo apt-get install -y wireguard-tools jq
          
          mkdir -p $HOME/wg
          echo "$WG_CONFIG" > $HOME/wg/wg0.conf
          chmod 600 $HOME/wg/wg0.conf
          
          # Явное отключение автоматического DNS в WG
          echo -e "\n[Interface]\nDNS = off" >> $HOME/wg/wg0.conf
          
          sudo wg-quick up $HOME/wg/wg0.conf
          
          # Принудительная настройка DNS
          echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf
          echo "nameserver 8.8.8.8" | sudo tee -a /etc/resolv.conf
          
          # Проверка подключения
          ping -c 3 google.com

      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'requirements-parser.txt'

      - name: Install parser dependencies
        run: |
          set -ex  # Включить подробное логирование и прерывание при ошибках

          # Проверка существования файла требований
          if [ ! -f "requirements-parser.txt" ]; then
            echo "::error::Файл requirements-parser.txt не найден в корне репозитория!"
            echo "Содержимое рабочей директории:"
            ls -la
            exit 1
          fi

          # Обновление pip
          python -m pip install --upgrade pip

          # Установка зависимостей с таймаутом и повторами
          for attempt in {1..5}; do
            echo "Попытка установки №$attempt"
            if pip install \
              --default-timeout=120 \
              -r requirements-parser.txt; then
              echo "Установка успешно завершена"
              break
            else
              echo "Ошибка установки, повтор через 20 секунд..."
              sleep 20
              [ $attempt -eq 5 ] && exit 1
            fi
          done

          # Проверка установленных пакетов
          echo "=== Установленные пакеты ==="
          pip list
          echo "=========================="

      - name: 🧠 Run parser
        run: python3 main.py --lang ru --posted-file articles/posted.json

      - name: Apply watermarks
        run: |
          pip install pillow==10.1.0
          python3 watermark_rotator.py

      - name: 🛑 Teardown WireGuard
        if: always()
        run: sudo wg-quick down $HOME/wg/wg0.conf || true

      - name: Upload parsed-content
        uses: actions/upload-artifact@v4
        with:
          name: parsed-content
          path: articles/*

  post:
    name: Publish new articles
    needs: parse
    runs-on: ubuntu-latest
    env:
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHANNEL: ${{ secrets.TELEGRAM_CHANNEL }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'requirements-poster.txt'

      - name: Install poster requirements
        run: |
          set -ex
          pip install -r requirements-poster.txt

      - name: Download parsed-content
        uses: actions/download-artifact@v4
        with:
          name: parsed-content
          path: parsed_articles

      - name: Run poster
        run: |
          python3 poster.py \
            --parsed-dir parsed_articles \
            --state-file articles/posted.json \
            --limit $BATCH_LIMIT

      - name: Commit updated posted.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add articles/posted.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update posted catalog after posting"
            git push
          fi
