name: VPN Parser & Poster (No Cache)

on:
  workflow_dispatch:

jobs:
  run-via-vpn:
    runs-on: ubuntu-latest

    steps:
    - name: 🔧 Установка зависимостей и утилит
      run: |
        run: |
        python3 poster.py
        sudo apt-get update
        sudo apt-get install -y wireguard-tools resolvconf curl python3-pip
        pip3 install requests beautifulsoup4 Pillow python-telegram-bot

    - name: 📁 Подготовка WireGuard-конфига
      run: |
        mkdir -p $HOME/wg
        echo "${{ secrets.WG_CONFIG }}" > $HOME/wg/wg0.conf
        chmod 600 $HOME/wg/wg0.conf

    - name: 🔐 Подключение VPN и DNS фиксация
      run: |
        sudo wg-quick up $HOME/wg/wg0.conf
        echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf
        echo "VPN IP: $(curl -s https://ipinfo.io/ip)"

    - name: 🧠 Запуск парсера (main.py)
      run: |
        echo "→ Запускаем main.py под VPN..."
        python3 main.py || echo "⚠️ Ошибка при выполнении парсера"

    - name: 🛑 Отключение VPN
      if: always()
      run: |
        echo "→ Отключаем WireGuard..."
        sudo wg-quick down $HOME/wg/wg0.conf || echo "WireGuard уже выключен"

    - name: 📤 Запуск постера (poster.py)
      run: |
        echo "→ Запускаем poster.py после отключения VPN..."
        python3 poster.py || echo "⚠️ Ошибка при выполнении постера"
